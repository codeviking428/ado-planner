# Public Entra client ID (not a secret). Register a Mobile and desktop app
# with redirect URI http://localhost and Azure DevOps delegated permissions.
# Leave empty to fall back to a PAT prompt at startup (stored with safeStorage).
ENTRA_CLIENT_ID=
MAIN_VITE_ENTRA_CLIENT_ID=
